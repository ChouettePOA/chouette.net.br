<hug webc:nokeep>
	<slot></slot>
</hug>

<style>
	hug {
		display: flex;
		gap: var(--space-m);
		justify-content: center;
	}
	hug.s > * {
		width: 23rem;
	}
	hug.m > * {
		width: var(--line-length);
	}
</style>
